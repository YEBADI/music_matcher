# Music Matcher System Design Document

### Architecture Overview

System Layers: A fully developed version of the Music Matcher app system is one composed of distinct layers for audio ingestion, fingerprinting, storage, and querying via an API. By separating out each of these concerns, we can then scale and maintain each part independently for better performance and reliability. Furhtermore, this design better supports distribyted teamwork efforts. An ingestion pipeline will handles new audio files, generating fingerprints and storing them. A matching engine takes an input clip and finds a matching song from the fingerprint database. The API layer (FastAPI) provides endpoints for uploading tracks to fingerprint and for querying matches. Although this is already implemented, this can be better modularised. Furthermore, we will also introduce background processing and queues to handle heavy audio computations without blocking user requests; improving responsiveness and throughput.

#### 1. Ingestion & Fingerprinting Pipeline

In the current implementation, the end user uploads the audio (MP3/WAV) via the FastAPI endpoint and it is immediately stored into memory, processed into a fingerprint via librosa and this fingerprint is stored. However, in a scaled up version of Music Matcher, we would decouple this process. Instead of processing immediately, the API can store the raw audio file in a dedicated S3 database and enqueue a fingerprinting task to a worker queue (e.g. via a Celery worker task, which would be appropriate here since this is already pythonic and Celery is coded in Python and can seamlessly integrate). The Celery background worker can pick up the task of retrieving the audio from the S3 database and then use librosa to compute the respective audio fingerprint.  By offloading this fingerprinting job to a worker process, the API responds immediately to the upload request with an acknowledgment, while the heavy lifting happens in the background (i.e. this decoupling ensures that the user isn’t stuck waiting on a long processing step). The fingerprint data (and the extracted metadata - such as the song title and length of the song) can then be stored in a dedicated fingerprint database. This fingerprint database can be a managed PostgreSQL (Amazon RDS) or Aurora instance, which efficiently indexes and queries fingerprint data (hence why this would be the prefered option over opting for a standard S3 service which does not provide processing) - i.e. storing fingerprints in a relational database or specialised search index allows fast lookup during matching. This async pipeline improves user experience and lets the system ingest songs in batch without overwhelming the web server.

#### 2. Audio Matching Workflow

In the current implementation, the matching workflow utilisies an immediate processing of the uploaded query file and matching - which means it is slow and has no fault tolerance. In a scaled up version, the API would accept the clip (saving temporarily to S3) and return a request ID. A Celery background worker can then processes the query clip to obtain a fingerprint and then search the fingerprint database for the best match (using cross-correlation). This search can be accelerated with proper indexing or using an algorithm optimized for audio fingerprints. Furthermore, decoupling the process means that we can implement batching of the fingerprint matching - this means fault tolerance is also introduced as, if a worker crashes, it does not halt the entire process. It also means that if multiple requests come in, it does not block the API. Everything is queued and workers can be scaled up accordingly.

#### 3. Scalable Storage & Fault Tolerance
* Audio and fingerprint storage: In the current implementation we are using simple in memory storage. A scaled up version needs persistent storage. Using S3 for object (audio file) storage and using a managed PostgreSQL (Amazon RDS) or Aurora instance (which regularly backs up and means high fault tolerance in case of system outage) is the optimal way to address the needs of the app. Storing audio files in S3 also allows for horizontal scaling as workers can access the data from a shared resource and not just simple memory.
* API Layer: The scaled up version of Music Matcher will have a stateless FastAPI (meaning it won't need to handle previous requests made to it and can handle each request indeopendelty). This allows for background scaling depending on the needs at the time. FastAPI and Uvicorn have the capability to provide this already.
* Asynchronous Processing: Decoupling processes and using Celery workers with queue management via AWS SQS means we can batch processes and can be reseliant to faults. 

### Cloud infrastructure and deployment

* AWS deployment: The design of Music Matcher has already implemented Dockerised container allowing for easy deployment on an AWS service such as Elastic Beanstalk (EB) or ECS with minial development effort. An optimal service for Music Matcher would be EB due to the proposed deisgn of horizontal scaling of background workers (depending on load). EB automatically handles provisioning EC2 instances, load balancing, auto-scaling, and can directly integrate with an RDS database. For background tasks, we can leverage EB’s Worker Environment tier. In EB, a Worker environment paired with an SQS queue can run our Celery workers: the web environment pushes tasks to SQS, and the worker environment listens to SQS and executes tasks. This setup, managed by EB, provides a simple way to do async processing with scalability (spin up more worker EC2 instances if the queue grows). EB simplifies many aspects (rolling updates, environment variables config, monitoring hooks) and would suit the 3 month timescale of pushing this current proof-of-concept to full scale production. Furthermore, using a tool like AWS CloudWatch can enable tracking of demand and memory usage and facilitate scaling of resources (we can set automatic alarms when specific thersholds are reached). This also enables us to implement logging of errors and check for any frequent problems for debugging / improvement. We can even set up custom CloudWatch metrics which can tell us about how long jobs are taking - to facilitate better development.
* Storage and database: As mentioned in 'Ingestion & Fingerprinting Pipeline', a dedicated S3 database and RDS relational database would be optimal for audio and fingerprint storage respectively.
* Secrets management: As in line wiht best practice - all credentials and keys (e.g. database credentials, AWS keys, API secrets) would be handled via AWS Secrets Manager and absolutely not hardcoded. This ensures sensitive information is encrypted at rest and only accessible to authorised services. The application can load secrets at startup via environment variables injected by the platform. This also means that switching credientials is smoother due to non-hardcoding.
* API security: Since a scaled up Music Matcher would be public facing, it is essential to implement safeguards. Depending on how the app would be used we could either use API authentication via API keys or require user accounts through OAuth. FastAPI has built in OAuth2PasswordBearer capabilities. Furthermore, we could implement rate limiting (X number of requests per user, implemented via API Gateway) and ensuring that HTTPS is used for security purposes.
* CI/CD: The current implementation already has some CI capabilities via GitHub Actions, with automated Pytest testing and Black formatting and Ruff linting for example. A scaled up version would have dedicated staging and production environments to let developers test features in a similar environment to production - before actually pushing to production. Furthermore, we could implement Docker Image generation upon pushing to main. The key design feature here is that due to the stateless nature of FastAPI and Celery workers, this means pushes to production do not induce downtime.

### Team Practices and Development Workflow

For scaling up within the tight timeframe of 3 months - some key team practices are essential for high performance development:

* Clear communication, documentation and teamwork: The team will maintain continious up-to-date documentation for the project. This includes a README for setup instructions, an API specification (e.g. using tools like FastAPI’s automatic docs), and possibly a developer guide covering the system architecture (like what you see in this document!). Each module and function should have clear docstrings explaining their purpose. We’ll also document decisions (ADR – Architecture Decision Records) for major choices (e.g., why we chose a particular queue or database) so future maintainers understand the context. Good documentation ensures onboarding new team members is easier and that knowledge isn’t lost if someone leaves or is off sick.

* Modular Codebase: The code must be broken down into clear modules. E.g. A fingerprinting module for fingerprinting logic, a module for database access (e.g. SQL for fingerprint queries), and the FastAPI app module for API routing. This separation makes the codebase easier to maintain and test. Each component should have its own unit testing developed and implemented. We’ll also design with extensibility in mind - e.g., the fingerprinting module could be swapped out for a more advanced algorithm in the future without affecting the API layer, as long as interfaces are consistent. This enables smoother development and improvement long-term.

* Coding Standards: To maintain consistency, the team will continue to use Black for automatic code formatting and Ruff for linting (as is already implemented). These tools ensure a uniform coding style and catch common mistakes or bad practices (we're all guilty at times!). We will enforce these via pre-commit hooks (so developers format / lint before pushing) and in CI pipelines (as is already implemented in testing). This reduces nitpicking in code reviews and lets reviewers focus on logic rather than style.

* Version Control and PRs: Development will follow a Git branching strategy (e.g. feature branches and pull requests off of the main branch). Every code change to main should go through a pull request and be reviewed by at least one other team member before merging. This practice protects the app and also improves code quality and knowledge sharing and making sure that the whole team is kept aware of any changes. We will set up branch protection so that pull requests must pass all CI checks and have an approving review before they can be merged. The pull request review process will include checking that new code has tests (if needed), is well-documented, and meets the acceptance criteria of the agile user story.

* Agile Process: The development will follow agile methodology. Blockers will be brought to the attention at the forefront of stand-ups. Features will be developed into user stories or tasks and planning will be done in sprints (e.g. two-week blocks of development). Regular sprint planning and retrospectives will be held, so the team can continuously improve its process. We’ll prioritise work based on aligning with overarching goals and also feedback – for instance, if the matching accuracy or performance is not meeting requirements, we schedule improvements in upcoming sprints. The team will also practice continuous improvement of the pipeline and infrastructure as needed (i.e. a DevOps mindset).

* Collaboration and Quality: In addition to pull request reviews, the team can do occasional paired programming on complex features (like optimising the fingerprint algorithm) to improve quality. We can also have regular knowledge sharing sessions to facilitate the team's overall understanding of new feature development and also improve capabilitires through new skill learning. We will utilise communications tools (e.g. Slack) for quick communication, but development updates must be recorded in the appropriate channels (jira / documentaiton) to effectively track progress and transparently keep abreast of all on-going development. Ensuring knowledge sharing is key - this can facilitate for more effecient development and avoid redundancy. This can also allow for identifying key areas where we can re-use code. It is crucial that code should be reviewed and documented for the team. We aim to foster a DevOps culture where developers are also responsible for the system’s reliability in production, not just writing code.

By adhering to these team practices (modular design, coding standards, code review and agile planning) we can create a codebase that is maintainable and a team that can sustainably add features or handle issues. Combined with the robust architecture and infrastructure choices described, Music Matcher can be a success!
